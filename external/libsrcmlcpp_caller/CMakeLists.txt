add_library(libsrcmlcpp_caller libsrcmlcpp_caller.cpp libsrcmlcpp_caller.h)
target_include_directories(libsrcmlcpp_caller PUBLIC ${CMAKE_CURRENT_LIST_DIR}/..)
# Under windows, auto __declspec(dllexport)
if (WIN32)
    set_target_properties(libsrcmlcpp_caller PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
endif()
target_link_libraries(libsrcmlcpp_caller PUBLIC libsrcml_static)
target_include_directories(libsrcmlcpp_caller PRIVATE ${CMAKE_CURRENT_LIST_DIR}/../srcML/src/parser)
